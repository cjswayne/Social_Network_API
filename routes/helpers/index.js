// error handling helper

module.exports = {
    handleError(err, res){


        // return res.json({
        //     error: 403,
        //     messages: Object.values(err.errors).map((val) => val.message),
        //   });
    }
}